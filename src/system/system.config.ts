import path from "path";

// const isDev = process.env.NODE_ENV === 'development';
const MIGRATIONS_FOLDER_PATH = "isDev"
//     ? './drizzle'
//     : path.join(process.resourcesPath, 'drizzle');
const APP_NAME = 'FleetControl'
export {
    MIGRATIONS_FOLDER_PATH,
    APP_NAME
}
